# layout-content-view (vendored)

Vendored from [p10ns11y/plugins/layout-content-view](https://github.com/p10ns11y/plugins/tree/main/layout-content-view).

Probe from repo root:

```bash
pnpm lcv:probe
VERIFY_FEATURE=/lab/landing-d pnpm lcv:probe
pnpm taste:check
```

Update vendor copy when the upstream plugin changes materially.
